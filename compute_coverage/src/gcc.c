#include "gcc.h"

int add(int a, int b) {
    return a + b;
}


// for f in overlap:
        // print( f[-1] )
    // try:
        // n = float( f[-1] )
        // if n <= 1 and n >= 0:
            // coverage_vec.append(n)
    //  except:
        // n = 0
        // coverage_vec.append(n)
//
//
//
